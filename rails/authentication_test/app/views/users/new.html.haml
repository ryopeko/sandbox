%h1 Sing up

%div.form-group
  = simple_form_for @user, html: { class: 'form-horizontal', role: 'form'}, wrapper: :horizontal_form do |f|
    = f.input :name
    = f.input :email
    = f.input :password
    = f.input :password_confirmation
    = f.button :submit, class: 'btn-primary'
